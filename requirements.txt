Flask==2.3.2
requests==2.32.3
gunicorn==23.0.0
python-dotenv==1.1.0   # if you’re loading from a .env locally (optional)
redis==5.0.4           # if you decide to switch to a Redis‐based state store in future
